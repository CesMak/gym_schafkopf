from setuptools import setup

setup(name='gym-schafkopf',
      version='0.0.1',
      install_requires=['gym', "numpy"]#And any other dependencies required
)
#Use: pip install -U gym==0.23.1
#otherwise there are many deprecated warnings!